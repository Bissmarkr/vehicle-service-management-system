import { Bell, Menu, Plus, Search } from 'lucide-react';

export default function AdminHeader({ auth, pendingRequests, onMenu, onNavigate }) {
  return <div className="admin-header-content"><button className="admin-mobile-menu" onClick={onMenu} type="button" aria-label="Open admin navigation"><Menu size={19} /></button><div className="admin-header-title"><span className="admin-eyebrow">ADMIN CONTROL CENTER</span><strong>Vehicle Service Management System</strong></div><label className="admin-global-search"><Search size={16} /><input placeholder="Search records" onKeyDown={(event) => { if (event.key === 'Enter') onNavigate('customers'); }} /><span>⌘ K</span></label><div className="admin-header-actions"><button className="admin-header-icon" onClick={() => onNavigate('payment-requests')} type="button" aria-label="Payment notifications"><Bell size={17} />{pendingRequests > 0 && <i>{pendingRequests}</i>}</button><button className="admin-quick-add" onClick={() => onNavigate('customers')} type="button"><Plus size={16} /> Quick add</button><div className="admin-user"><div className="admin-avatar">{auth?.name?.slice(0, 1).toUpperCase() || 'A'}</div><span><strong>{auth?.name || 'Administrator'}</strong><small>Administrator</small></span></div></div></div>;
}
